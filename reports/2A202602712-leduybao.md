# Individual contribution report

---

## Thông tin

- Họ và tên: Lê Duy Bảo
- Mã học viên: 2A202602749
- Nhóm: BLS
- Repository/branch: https://github.com/brxcewxyne/K4-L3B-RAG-Pipeline.git (`main`)

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Evaluation — Golden dataset (18 Q&A) | Biên soạn 18 cặp hỏi–đáp grounded trên toàn bộ 9 tài liệu standardized (3 legal + 6 news, 14 câu VI + 4 câu EN khớp doc EN), mỗi case có `question`/`expected_answer`/`expected_context` | `group_project/evaluation/golden_dataset.json` | Done |
| Evaluation — 4 metrics + A/B | Viết evaluator offline tái dùng code thật (`chunk_documents`, `lexical_search`, `rerank_rrf`), so sánh dense-only (avg 0.9197) vs hybrid+RRF (avg 0.9300), hiệu chỉnh threshold 0.1467 bằng probe in/out-of-domain | `group_project/evaluation/evaluate_offline.py`, `group_project/evaluation/evaluation_results.json` | Done |
| Evaluation — Báo cáo kết quả | Viết `RESULT.md` đầy đủ (run info, overall scores, A/B, 3 worst performers, 3 recommendations, bonus RRF k=10 vs 60), không còn placeholder | `reports/RESULT.md` (+ bản copy `group_project/evaluation/RESULT.md` cho acceptance test) | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Đánh giá offline bằng TF-IDF proxy thay vì gọi embedding/LLM thật, nhưng tái dùng nguyên code chunking/BM25/RRF của repo.
   **Lý do/evidence:** Môi trường chấm không có `OPENAI_API_KEY` (`.env` trống) và contract test cấm gọi network; proxy vẫn tôn trọng `SearchResult` contract và luật fallback dùng dense-score gốc. Kết quả tái chạy xác định: hybrid 0.9300 > dense 0.9197.
   **Trade-off:** Proxy đơn ngữ nên đánh giá thấp khả năng xuyên ngữ VI↔EN của embedding thật — đã ghi rõ trong report và đề xuất chạy lại khi có key.

2. **Quyết định:** Golden dataset 18 câu (vượt yêu cầu 15), phủ cả 9 doc, 4/18 câu viết bằng tiếng Anh theo đúng ngôn ngữ doc hỗ trợ.
   **Lý do/evidence:** Lần chạy đầu 18 câu thuần Việt chỉ đạt avg ~0.76 vì 4 doc EN (`pubg_ban_penalty_information_vi`, `pubg_account_cheating_responsibility_vi`...) không khớp từ vựng với proxy; chuyển 4 câu sang EN đưa recall các câu này về ~1.0 và avg lên 0.93.
   **Trade-off:** Golden nghiêng về câu cùng ngôn ngữ doc, chưa đo sức xuyên ngữ của hệ thật — để lại cho lần eval có embedding thật.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `python group_project/evaluation/evaluate_offline.py` (18 golden × 2 config, top_k=5) + probe out-of-domain ("Súng nào mạnh nhất trong PUBG hiện tại?", "Thời tiết Hà Nội...?", "Cách nấu phở...?") để hiệu chỉnh threshold; `pytest tests/test_acceptance.py::test_golden_dataset_has_15_grounded_cases` và `test_evaluation_report_is_completed`.
- Kết quả trước/sau nếu có: trước (18 câu VI, relevance = phủ câu hỏi trên top-1): avg 0.76 → sau (4 câu EN + relevance trên top-5 context): dense 0.9197, hybrid 0.9300 (faithfulness 0.9556, precision 1.0 cả hai).
- Lỗi đã phát hiện và cách xử lý: G08 top-1 nhầm văn bản legal (3 doc legal dùng chéo thuật ngữ) và G14 top-1 nguồn thứ cấp thay vì thông báo KRAFTON → ghi vào Worst performers + recommendation authority-aware ranking, không sửa code pipeline (ngoài phạm vi phần việc).

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Faithfulness/Answer relevance là proxy không dùng LLM judge nên chưa đo được ảo giác sinh văn của `generate_with_citation` thật.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Chạy eval end-to-end có LLM judge (faithfulness theo claim, relevance theo RAGAS) với `OPENAI_API_KEY` thật trên cùng golden để đối chiếu với số offline.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Lê Duy Bảo
